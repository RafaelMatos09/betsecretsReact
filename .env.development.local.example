# Usar backend no Render em vez do local (quando não quiser rodar dotnet run)
# VITE_API_PROXY_TARGET=https://betsecretsc.onrender.com

# Se o backend local estiver na porta 8080 (processo antigo), use:
# VITE_API_PROXY_TARGET=http://localhost:8080
